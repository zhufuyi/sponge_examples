// Code generated by https://github.com/zhufuyi/sponge
// Test_service_comment_methods is used to test the comment api
// Test_service_comment_benchmark is used to performance test the comment api

package service

import (
	"context"
	"encoding/json"
	"fmt"
	"testing"
	"time"

	"github.com/zhufuyi/sponge/pkg/grpc/benchmark"

	commentV1 "eshop/api/comment/v1"
	"eshop/comment/configs"
	"eshop/comment/internal/config"
)

// Test service comment api via grpc client
func Test_service_comment_methods(t *testing.T) {
	conn := getRPCClientConnForTest()
	cli := commentV1.NewCommentClient(conn)
	ctx, _ := context.WithTimeout(context.Background(), time.Second*30)

	tests := []struct {
		name    string
		fn      func() (interface{}, error)
		wantErr bool
	}{

		{
			name: "ListByProductID",
			fn: func() (interface{}, error) {
				// todo type in the parameters before testing
				req := &commentV1.ListByProductIDRequest{
					ProductID: 0,
				}

				return cli.ListByProductID(ctx, req)
			},
			wantErr: false,
		},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, err := tt.fn()
			if (err != nil) != tt.wantErr {
				t.Errorf("test '%s' error = %v, wantErr %v", tt.name, err, tt.wantErr)
				return
			}
			data, _ := json.MarshalIndent(got, "", "    ")
			fmt.Println(string(data))
		})
	}
}

// performance test service comment api, copy the report to
// the browser to view when the pressure test is finished.
func Test_service_comment_benchmark(t *testing.T) {
	err := config.Init(configs.Path("comment.yml"))
	if err != nil {
		panic(err)
	}

	grpcClientCfg := getGRPCClientCfg()
	host := fmt.Sprintf("%s:%d", grpcClientCfg.Host, grpcClientCfg.Port)
	protoFile := configs.Path("../api/comment/v1/comment.proto")
	// If third-party dependencies are missing during the press test,
	// copy them to the project's third_party directory.
	dependentProtoFilePath := []string{
		configs.Path("../third_party"), // third_party directory
		configs.Path(".."),             // Previous level of third_party
	}

	tests := []struct {
		name    string
		fn      func() error
		wantErr bool
	}{

		{
			name: "ListByProductID",
			fn: func() error {
				// todo type in the parameters before benchmark testing
				message := &commentV1.ListByProductIDRequest{
					ProductID: 0,
				}
				total := 1000 // total number of requests

				b, err := benchmark.New(host, protoFile, "ListByProductID", message, dependentProtoFilePath, total)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			err := tt.fn()
			if (err != nil) != tt.wantErr {
				t.Errorf("test '%s' error = %v, wantErr %v", tt.name, err, tt.wantErr)
				return
			}
		})
	}
}
