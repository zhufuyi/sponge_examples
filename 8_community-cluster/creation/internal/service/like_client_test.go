// Code generated by https://github.com/zhufuyi/sponge

package service

import (
	"context"
	"encoding/json"
	"fmt"
	"testing"
	"time"

	creationV1 "creation/api/creation/v1"
	"creation/configs"
	"creation/internal/config"

	"github.com/zhufuyi/sponge/pkg/grpc/benchmark"
)

// Test each method of likeService via the rpc client
func Test_service_likeService_methods(t *testing.T) {
	conn := getRPCClientConnForTest()
	cli := creationV1.NewLikeServiceClient(conn)
	ctx, _ := context.WithTimeout(context.Background(), time.Second*5)

	tests := []struct {
		name    string
		fn      func() (interface{}, error)
		wantErr bool
	}{
		{
			name: "Create",
			fn: func() (interface{}, error) {
				// todo type in the parameters to test
				req := &creationV1.CreateLikeRequest{
					UserId: 0, 
					ObjType: 0, 
					ObjId: 0, 
				}
				return cli.Create(ctx, req)
			},
			wantErr: false,
		},
		{
			name: "Delete",
			fn: func() (interface{}, error) {
				// todo type in the parameters to test
				req := &creationV1.DeleteLikeRequest{
					UserId: 0, 
					ObjType: 0, 
					ObjId: 0, 
				}
				return cli.Delete(ctx, req)
			},
			wantErr: false,
		},
		{
			name: "ListPost",
			fn: func() (interface{}, error) {
				// todo type in the parameters to test
				req := &creationV1.ListPostLikeRequest{
					PostId: 0, 
					Page: 0, 
					Limit: 0, 
				}
				return cli.ListPost(ctx, req)
			},
			wantErr: false,
		},
		{
			name: "ListComment",
			fn: func() (interface{}, error) {
				// todo type in the parameters to test
				req := &creationV1.ListCommentLikeRequest{
					CommentId: 0, 
					Page: 0, 
					Limit: 0, 
				}
				return cli.ListComment(ctx, req)
			},
			wantErr: false,
		},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, err := tt.fn()
			if (err != nil) != tt.wantErr {
				t.Errorf("test '%s' error = %v, wantErr %v", tt.name, err, tt.wantErr)
				return
			}
			data, _ := json.MarshalIndent(got, "", "    ")
			fmt.Println(string(data))
		})
	}
}

// Perform a stress test on likeService's method and 
// copy the press test report to your browser when you are finished.
func Test_service_likeService_benchmark(t *testing.T) {
	err := config.Init(configs.Path("creation.yml"))
	if err != nil {
		panic(err)
	}
	host := fmt.Sprintf("127.0.0.1:%d", config.Get().Grpc.Port)
	protoFile := configs.Path("../api/creation/v1/like.proto")
	// If third-party dependencies are missing during the press test,
	// copy them to the project's third_party directory.
	importPaths := []string{
		configs.Path("../third_party"), // third_party directory
		configs.Path(".."),             // Previous level of third_party
	}

	tests := []struct {
		name    string
		fn      func() error
		wantErr bool
	}{
		{
			name: "Create",
			fn: func() error {
				// todo type in the parameters to test
				message := &creationV1.CreateLikeRequest{
					UserId: 0, 
					ObjType: 0, 
					ObjId: 0, 
				}
				var total uint = 1000 // total number of requests
				b, err := benchmark.New(host, protoFile, "Create", message, total, importPaths...)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},
		{
			name: "Delete",
			fn: func() error {
				// todo type in the parameters to test
				message := &creationV1.DeleteLikeRequest{
					UserId: 0, 
					ObjType: 0, 
					ObjId: 0, 
				}
				var total uint = 1000 // total number of requests
				b, err := benchmark.New(host, protoFile, "Delete", message, total, importPaths...)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},
		{
			name: "ListPost",
			fn: func() error {
				// todo type in the parameters to test
				message := &creationV1.ListPostLikeRequest{
					PostId: 0, 
					Page: 0, 
					Limit: 0, 
				}
				var total uint = 1000 // total number of requests
				b, err := benchmark.New(host, protoFile, "ListPost", message, total, importPaths...)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},
		{
			name: "ListComment",
			fn: func() error {
				// todo type in the parameters to test
				message := &creationV1.ListCommentLikeRequest{
					CommentId: 0, 
					Page: 0, 
					Limit: 0, 
				}
				var total uint = 1000 // total number of requests
				b, err := benchmark.New(host, protoFile, "ListComment", message, total, importPaths...)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			err := tt.fn()
			if (err != nil) != tt.wantErr {
				t.Errorf("test '%s' error = %v, wantErr %v", tt.name, err, tt.wantErr)
				return
			}
		})
	}
}
