// Code generated by https://github.com/zhufuyi/sponge

package service

import (
	"context"
	"encoding/json"
	"fmt"
	"testing"
	"time"

	relationV1 "relation/api/relation/v1"
	"relation/configs"
	"relation/internal/config"

	"github.com/zhufuyi/sponge/pkg/grpc/benchmark"
)

// Test each method of relationService via the rpc client
func Test_service_relationService_methods(t *testing.T) {
	conn := getRPCClientConnForTest()
	cli := relationV1.NewRelationServiceClient(conn)
	ctx, _ := context.WithTimeout(context.Background(), time.Second*5)

	tests := []struct {
		name    string
		fn      func() (interface{}, error)
		wantErr bool
	}{
		{
			name: "Follow",
			fn: func() (interface{}, error) {
				// todo type in the parameters to test
				req := &relationV1.FollowRequest{
					UserId: 0, 
					FollowedUid: 0, 
				}
				return cli.Follow(ctx, req)
			},
			wantErr: false,
		},
		{
			name: "Unfollow",
			fn: func() (interface{}, error) {
				// todo type in the parameters to test
				req := &relationV1.UnfollowRequest{
					UserId: 0, 
					FollowedUid: 0, 
				}
				return cli.Unfollow(ctx, req)
			},
			wantErr: false,
		},
		{
			name: "ListFollowing",
			fn: func() (interface{}, error) {
				// todo type in the parameters to test
				req := &relationV1.ListFollowingRequest{
					UserId: 0, 
					Page: 0, 
					Limit: 0, 
				}
				return cli.ListFollowing(ctx, req)
			},
			wantErr: false,
		},
		{
			name: "ListFollower",
			fn: func() (interface{}, error) {
				// todo type in the parameters to test
				req := &relationV1.ListFollowerRequest{
					UserId: 0, 
					Page: 0, 
					Limit: 0, 
				}
				return cli.ListFollower(ctx, req)
			},
			wantErr: false,
		},
		{
			name: "BatchGetRelation",
			fn: func() (interface{}, error) {
				// todo type in the parameters to test
				req := &relationV1.BatchGetRelationRequest{
					UserId: 0, 
					Uids: nil, 
				}
				return cli.BatchGetRelation(ctx, req)
			},
			wantErr: false,
		},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, err := tt.fn()
			if (err != nil) != tt.wantErr {
				t.Errorf("test '%s' error = %v, wantErr %v", tt.name, err, tt.wantErr)
				return
			}
			data, _ := json.MarshalIndent(got, "", "    ")
			fmt.Println(string(data))
		})
	}
}

// Perform a stress test on relationService's method and 
// copy the press test report to your browser when you are finished.
func Test_service_relationService_benchmark(t *testing.T) {
	err := config.Init(configs.Path("relation.yml"))
	if err != nil {
		panic(err)
	}
	host := fmt.Sprintf("127.0.0.1:%d", config.Get().Grpc.Port)
	protoFile := configs.Path("../api/relation/v1/relation.proto")
	// If third-party dependencies are missing during the press test,
	// copy them to the project's third_party directory.
	importPaths := []string{
		configs.Path("../third_party"), // third_party directory
		configs.Path(".."),             // Previous level of third_party
	}

	tests := []struct {
		name    string
		fn      func() error
		wantErr bool
	}{
		{
			name: "Follow",
			fn: func() error {
				// todo type in the parameters to test
				message := &relationV1.FollowRequest{
					UserId: 0, 
					FollowedUid: 0, 
				}
				var total uint = 1000 // total number of requests
				b, err := benchmark.New(host, protoFile, "Follow", message, total, importPaths...)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},
		{
			name: "Unfollow",
			fn: func() error {
				// todo type in the parameters to test
				message := &relationV1.UnfollowRequest{
					UserId: 0, 
					FollowedUid: 0, 
				}
				var total uint = 1000 // total number of requests
				b, err := benchmark.New(host, protoFile, "Unfollow", message, total, importPaths...)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},
		{
			name: "ListFollowing",
			fn: func() error {
				// todo type in the parameters to test
				message := &relationV1.ListFollowingRequest{
					UserId: 0, 
					Page: 0, 
					Limit: 0, 
				}
				var total uint = 1000 // total number of requests
				b, err := benchmark.New(host, protoFile, "ListFollowing", message, total, importPaths...)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},
		{
			name: "ListFollower",
			fn: func() error {
				// todo type in the parameters to test
				message := &relationV1.ListFollowerRequest{
					UserId: 0, 
					Page: 0, 
					Limit: 0, 
				}
				var total uint = 1000 // total number of requests
				b, err := benchmark.New(host, protoFile, "ListFollower", message, total, importPaths...)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},
		{
			name: "BatchGetRelation",
			fn: func() error {
				// todo type in the parameters to test
				message := &relationV1.BatchGetRelationRequest{
					UserId: 0, 
					Uids: nil, 
				}
				var total uint = 1000 // total number of requests
				b, err := benchmark.New(host, protoFile, "BatchGetRelation", message, total, importPaths...)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			err := tt.fn()
			if (err != nil) != tt.wantErr {
				t.Errorf("test '%s' error = %v, wantErr %v", tt.name, err, tt.wantErr)
				return
			}
		})
	}
}
