// Code generated by https://github.com/zhufuyi/sponge

package routers

import (
	"context"

	"github.com/gin-gonic/gin"
	"google.golang.org/grpc/metadata"

	"github.com/zhufuyi/sponge/pkg/gin/middleware"
	"github.com/zhufuyi/sponge/pkg/logger"

	productV1 "eshop/api/product/v1"
	"eshop/product/internal/handler"
)

func init() {
	allMiddlewareFns = append(allMiddlewareFns, func(c *middlewareConfig) {
		productMiddlewares(c)
	})

	allRouteFns = append(allRouteFns,
		func(r *gin.Engine, groupPathMiddlewares map[string][]gin.HandlerFunc, singlePathMiddlewares map[string][]gin.HandlerFunc) {
			productRouter(r, groupPathMiddlewares, singlePathMiddlewares, handler.NewProductHandler())
		})
}

func productRouter(
	r *gin.Engine,
	groupPathMiddlewares map[string][]gin.HandlerFunc,
	singlePathMiddlewares map[string][]gin.HandlerFunc,
	iService productV1.ProductLogicer) {
	ctxFn := func(c *gin.Context) context.Context {
		md := metadata.New(map[string]string{
			middleware.ContextRequestIDKey: middleware.GCtxRequestID(c),
		})
		return metadata.NewIncomingContext(c.Request.Context(), md)
	}
	productV1.RegisterProductRouter(
		r,
		groupPathMiddlewares,
		singlePathMiddlewares,
		iService,
		productV1.WithProductLogger(logger.Get()),
		productV1.WithProductRPCResponse(),
		productV1.WithProductWrapCtx(ctxFn),
		productV1.WithProductErrorToHTTPCode(
		// Set some error codes to standard http return codes,
		// by default there is already ecode.InternalServerError and ecode.ServiceUnavailable
		// example:
		// 	ecode.Forbidden, ecode.LimitExceed,
		),
	)
}

// you can set the middleware of a route group, or set the middleware of a single route,
// or you can mix them, pay attention to the duplication of middleware when mixing them,
// it is recommended to set the middleware of a single route in preference
func productMiddlewares(c *middlewareConfig) {
	// set up group route middleware, group path is left prefix rules,
	// if the left prefix is hit, the middleware will take effect, e.g. group route is /api/v1, route /api/v1/product/:id  will take effect
	// c.setGroupPath("/api/v1/product", middleware.Auth())

	// set up single route middleware, just uncomment the code and fill in the middlewares, nothing else needs to be changed
	//c.setSinglePath("POST", "/api/v1/product", middleware.Auth())    // Create product
	//c.setSinglePath("DELETE", "/api/v1/product/:id", middleware.Auth())    // DeleteByID delete product by id
	//c.setSinglePath("PUT", "/api/v1/product/:id", middleware.Auth())    // UpdateByID update product by id
	//c.setSinglePath("GET", "/api/v1/product/:id", middleware.Auth())    // GetByID get product by id
	//c.setSinglePath("POST", "/api/v1/product/list", middleware.Auth())    // List of product by query parameters
}
