// Code generated by https://github.com/zhufuyi/sponge

package service

import (
	"context"
	"encoding/json"
	"fmt"
	"testing"
	"time"

	stockV1 "stock/api/stock/v1"
	"stock/configs"
	"stock/internal/config"

	"github.com/zhufuyi/sponge/pkg/grpc/benchmark"
)

// Test each method of stock via the rpc client
func Test_service_stock_methods(t *testing.T) {
	conn := getRPCClientConnForTest()
	cli := stockV1.NewStockClient(conn)
	ctx, _ := context.WithTimeout(context.Background(), time.Second*5)

	tests := []struct {
		name    string
		fn      func() (interface{}, error)
		wantErr bool
	}{
		{
			name: "StockDeduct",
			fn: func() (interface{}, error) {
				// todo type in the parameters to test
				req := &stockV1.StockDeductRequest{
					ProductId: 0, 
					ProductCount: 0, 
				}
				return cli.StockDeduct(ctx, req)
			},
			wantErr: false,
		},
		{
			name: "StockDeductRevert",
			fn: func() (interface{}, error) {
				// todo type in the parameters to test
				req := &stockV1.StockDeductRevertRequest{
					ProductId: 0, 
					ProductCount: 0, 
				}
				return cli.StockDeductRevert(ctx, req)
			},
			wantErr: false,
		},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got, err := tt.fn()
			if (err != nil) != tt.wantErr {
				t.Errorf("test '%s' error = %v, wantErr %v", tt.name, err, tt.wantErr)
				return
			}
			data, _ := json.MarshalIndent(got, "", "    ")
			fmt.Println(string(data))
		})
	}
}

// Perform a stress test on stock's method and 
// copy the press test report to your browser when you are finished.
func Test_service_stock_benchmark(t *testing.T) {
	err := config.Init(configs.Path("stock.yml"))
	if err != nil {
		panic(err)
	}
	host := fmt.Sprintf("127.0.0.1:%d", config.Get().Grpc.Port)
	protoFile := configs.Path("../api/stock/v1/stock.proto")
	// If third-party dependencies are missing during the press test,
	// copy them to the project's third_party directory.
	importPaths := []string{
		configs.Path("../third_party"), // third_party directory
		configs.Path(".."),             // Previous level of third_party
	}

	tests := []struct {
		name    string
		fn      func() error
		wantErr bool
	}{
		{
			name: "StockDeduct",
			fn: func() error {
				// todo type in the parameters to test
				message := &stockV1.StockDeductRequest{
					ProductId: 0, 
					ProductCount: 0, 
				}
				var total uint = 1000 // total number of requests
				b, err := benchmark.New(host, protoFile, "StockDeduct", message, total, importPaths...)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},
		{
			name: "StockDeductRevert",
			fn: func() error {
				// todo type in the parameters to test
				message := &stockV1.StockDeductRevertRequest{
					ProductId: 0, 
					ProductCount: 0, 
				}
				var total uint = 1000 // total number of requests
				b, err := benchmark.New(host, protoFile, "StockDeductRevert", message, total, importPaths...)
				if err != nil {
					return err
				}
				return b.Run()
			},
			wantErr: false,
		},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			err := tt.fn()
			if (err != nil) != tt.wantErr {
				t.Errorf("test '%s' error = %v, wantErr %v", tt.name, err, tt.wantErr)
				return
			}
		})
	}
}
